#include<stdio.h>
int main()
{
    int radius;
    printf("enter the radius of circle");
    scanf("%d", &radius);
    printf("the area of circle is %f", 3.14 * radius * radius);

    return 0;
}