#include<stdio.h>
int main()
{
    int a,b,formula;
    printf("enter first number:");
    scanf("%d",&a);
    printf("enter second number:");
    scanf("%d",&b);
    formula=(a*a)+(b*b);
    printf("formula= %d",formula);

    return 0;
}
